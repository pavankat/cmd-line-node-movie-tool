* to run program

```node code.js titanic```

* steps to replicate

* npm init
* npm install request --save
* touch code.js
* open up code.js and copy and paste sample code from request library page
* replace URL to hit with omdb api url call

* use arguments passed to the script using node's process library - which is built in
	* https://docs.nodejitsu.com/articles/command-line/how-to-parse-command-line-arguments